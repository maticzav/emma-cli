# Migration `20191027200659-add-signature-field`

This migration has been generated by maticzav at 10/27/2019, 8:06:59 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Starter" ADD COLUMN "signature" text NOT NULL DEFAULT '' ;

CREATE UNIQUE INDEX "Starter.signature" ON "public"."Starter"("signature")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191025200318-init-emma..20191027200659-add-signature-field
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource pg {
   provider = "postgres"
-  url = "***"
+  url      = env("POSTGRESQL_URL")
 }
 generator photon {
   provider = "photonjs"
@@ -13,8 +13,9 @@
   id           String   @id @default(cuid())
   createdAt    DateTime @default(now())
   updatedAt    DateTime @updatedAt
   // Meta
+  signature    String   @unique
   repo         String
   owner        String
   // Info
   path         String
```

## Photon Usage

You can use a specific Photon built for this migration (20191027200659-add-signature-field)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191027200659-add-signature-field'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()
```
