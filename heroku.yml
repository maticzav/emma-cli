setup:
  addons:
    - plan: heroku-postgresql
build:
  docker:
    web: server/Dockerfile
release:
  image: web
  command:
    - ./scripts/migrate.sh
run:
  web: ./node_modules/.bin/probot run server/dist/index.js -p $PORT
