setup:
  addons:
    - plan: heroku-postgresql
build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - |
      echo "FILES"
      pwd
      ls .
      echo "SCRIPTS"
      ls ./scripts
      echo "MIGRAYE"
      ./scripts/migrate.sh
run:
  web: ./node_modules/.bin/probot run ./server/dist/index.js -p $PORT
