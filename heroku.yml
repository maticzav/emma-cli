setup:
  addons:
    - plan: heroku-postgresql
build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - |
      ls .
      pwd
run:
  web: ./node_modules/.bin/probot run server/dist/index.js -p $PORT
